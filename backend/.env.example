# Database Configuration
# Option 1: Use remote database (Supabase) - Recommended for local development
DATABASE_URL=postgresql://user:password@host:port/database

# Option 2: Use individual database variables (for local PostgreSQL)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=rc_cafe
# DB_USER=postgres
# DB_PASSWORD=postgres

# Server Configuration
PORT=5001
NODE_ENV=development

# JWT Secret (generate a random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email Configuration (SMTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@furyroad.com

# Default Admin Password (used when creating default admin user)
DEFAULT_ADMIN_PASSWORD=admin123
