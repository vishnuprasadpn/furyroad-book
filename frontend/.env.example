# Frontend API Configuration
# For local development, point to your local backend
# If not set, automatically detects localhost and uses http://localhost:5001/api
VITE_API_URL=http://localhost:5001

# If not set and not on localhost, defaults to live backend: https://furyroad-backend.fly.dev/api
